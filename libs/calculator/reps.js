/**
 * Calculate the repetition cost
 *
 * @param {number} total_cost - total cost
 * @param {number} number_reps - amount of repetitions (ej. 2)
 *
 * @returns {number} new cost
 */
export function calculateRepsCost(total_cost, number_reps) {
  let total = 0;

  return total;
}
